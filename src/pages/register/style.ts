import { CSSProperties } from "react";
export const title: CSSProperties = {
    textAlign: "center",
    marginBottom: "20px",
  };
export const check: CSSProperties = {
    marginBottom: "20px",
    display: "flex",
    justifyContent: "left",
  };