import { CSSProperties } from 'react'

export const cancel: CSSProperties = {
  minWidth: '150px',
  minHeight: '40px',
  fontSize: '15px',
}
export const save: CSSProperties = {
  minWidth: '150px',
  minHeight: '40px',
  fontSize: '15px',
}
